# Publishing

```
# Project Build
 1. npm install
 2. npm run dev
```

```
# 작업 방식
 1. ANYAD/styles 폴더에 컴포넌트 이름의 scss파일 생성
 2. 해당 컴포넌트에 import 
```

```
# 작업 참고 사항
 1. id, class 명은 snake case 사용
   - 모든 이름은 소문자로 작성
   - 단어의 구분이 있을 경우 _ 사용
 ex) firstdiv -> first_div
```

```
# 과제 제출
 1. publishing은 팀 없이 개인으로 진행
 2. Branch는 각자 자기 이름으로 생성 후 PR
```
